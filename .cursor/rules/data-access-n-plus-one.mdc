---
description: 数据读取与关联加载时避免 N+1 查询（批量、JOIN、预加载）
alwaysApply: true
---

# 数据访问：避免 N+1

## 问题

**N+1**：先 **1 次**查询列表，再在循环里对每条各查 **1 次**关联，总查询约 **1 + N** 次。

## 编写与评审时的要求

- **禁止**在循环内对同一数据源发起**逐条**独立查询（除非已确认规模极小且非热路径，并注明）。
- 优先：**JOIN / include / eager load**、**批量 `IN (...)` + 内存组装**、或 DataLoader 式合并请求。

## 自检

列表/详情接口：**主查询次数 + 关联查询次数**是否与列表长度无关（或仅常数级）？
