---
description: HTTP 安全头、输入校验、日志脱敏、数值与复杂度边界（对齐 sentinel-monorepo）
alwaysApply: true
---

# 安全编码与稳健性

## 接口与 HTTP：安全响应头与配置

新增或调整 **HTTP 服务**时，在**统一入口**配置常见安全头，并与部署方式一致：

- **建议覆盖**：`Strict-Transport-Security`（HTTPS）、`X-Content-Type-Options: nosniff`、`X-Frame-Options` 或 CSP、`Referrer-Policy`、`Content-Security-Policy`（按业务收紧）。
- **配套**：合理 **CORS**（勿 `*` 搭配凭据）、**速率限制**、敏感接口鉴权；生产强制 **HTTPS**。

若仓库已有安全中间件或网关规则，**优先复用并扩展**，避免在各路由零散手写。

## 通用：减少攻击面

- **输入**：外部入口（query、body、header、路径参数）做**类型与范围校验**（如 zod）。
- **注入**：数据库访问**参数化**；慎拼接命令行。
- **XSS**：慎用工 `dangerouslySetInnerHTML`；后端勿原样输出不可信 HTML。
- **敏感信息**：错误响应与日志**不返回**堆栈、内部路径、密钥片段；见 `env-secrets.mdc`。

## 数据越界与数值安全

- **下标与长度**：访问前确认合法；分页参数设 **上限**。
- **数值**：注意精度与溢出；除法注意 **除零**。
- **时间与资源**：循环、队列设边界，避免无界占用内存。

## 算法复杂度

数据规模可能很大时避免 **O(n²)** 暴力；优先滑动窗口、双指针、前缀和等；必要时说明**规模上界**。
