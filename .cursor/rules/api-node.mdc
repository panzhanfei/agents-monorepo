---
description: apps/api（Express）目录职责、请求链路与扩展方式
globs: apps/api/**/*.ts
alwaysApply: false
---

# apps/api 结构与分层

本服务为 **Express** 应用：**`src/index.ts`** 内定义 **`createApp`**、**`gracefulShutdown`**，并启动 HTTP **`listen`** 与进程信号处理；**不再**使用单独的 `server.ts`。安全头使用 **helmet**，CORS 与 JSON 限额在同文件配置。

**BullMQ Worker**：入口为 **`src/worker/index.ts`**，与队列任务实现 **`src/worker/jobs/`**（如 `processAgentTask`）同目录树，便于后台消费逻辑聚合。**`src/queue/`** 仍为队列封装（如 `agentQueue`），由业务与 worker 引用。

## 按模块划分子目录（controllers / models / services / routes / views）

同一业务模块在 **controller / model / service / route / view** 五层下的路径应对齐（示例）：

| 模块 | `controllers/` | `models/` | `services/` | `routes/` | `views/` |
|------|----------------|-----------|-------------|-----------|----------|
| 认证 | `controllers/auth/` | `models/auth/` | `services/auth/` | `routes/auth/` | （可无） |
| 项目 | `controllers/project/` | `models/project/` | `services/project/` | `routes/project/` | `views/project/` |
| 项目内对话 | `controllers/project-chat/` | `models/project-chat/` | `services/project-chat/` | `routes/project-chat/` | `views/project-chat/` |
| … | … | … | … | … | … |

各模块目录以 **`index.ts`** 为入口聚合导出；**`runner-v1`** 的视图可含多个文件并由 **`views/runner-v1/index.ts`** 再导出。

## `lib/` 子目录（按域拆分 + 根聚合）

**`lib/`** 内按 **`common` / `auth` / `agent` / `runner`** 分子目录（各含 **`index.ts`**），**`lib/index.ts`** 统一 **`export *`**，业务侧仍可只写 **`import { … } from "@/lib"`**：

| 子目录 | 内容示例 |
|--------|-----------|
| **`lib/common/`** | `prisma`、`redis`、`httpError`、`requestScope` |
| **`lib/auth/`** | `jwt`、`password`、`refreshTokenSession`、`authUserPayload` |
| **`lib/agent/`** | `agentInference`、`agentModels`、`agentSlotMerge`、`inferenceProbe` |
| **`lib/runner/`** | `runnerCredentials`、`runnerOnline` |

新增 **`lib/<子目录>`** 时，同步 **`scripts/fix-barrel-imports.mjs`** 内 **`nestedIndexLeaves.lib`**。

## 请求链路（MVC + Services）

1. **`src/routes/<模块>/`**：仅 **`Router`**、路径与中间件顺序，绑定 **controller**。
2. **`src/controllers/<模块>/`**：校验、读写 `req`/`res`；只调 **`services`**。
3. **`src/services/<模块>/`**：用例编排（**model**、`lib`、`config`、`queue`、**`HttpError`**）。
4. **`src/models/<模块>/`**：Prisma 与事务数据层。
5. **`src/views/<模块>/`**：响应形状与 DTO。
6. **`src/middleware/`**、**`src/lib/`**：中间件与基础设施。

**只读聚合**：在 **service + model**（必要时 + view）中完成；遵守 N+1 与事务约定。

## 目录职责（与当前仓库一致）

| 目录 | 职责 |
|------|------|
| **`src/config/`** | 环境与服务配置。 |
| **`src/controllers/`** | 按子目录分模块的 HTTP 控制器。 |
| **`src/services/`** | 按子目录分模块的用例层。 |
| **`src/models/`** | 按子目录分模块的数据访问层。 |
| **`src/views/`** | 按子目录分模块的响应形状。 |
| **`src/lib/`** | **`common` / `auth` / `agent` / `runner` 子目录** + 根 **`index.ts`** 聚合导出。 |
| **`src/middleware/`** | Express 中间件。 |
| **`src/routes/`** | 按子目录分模块的路由；**路由根 `routes/index.ts`** 聚合导出。 |
| **`src/queue/`** | BullMQ 等队列封装。 |
| **`src/worker/`** | Worker 进程入口 **`index.ts`**；**`worker/jobs/`** 为任务处理实现。 |
| **`src/types/`** | 类型补充。 |
| **`src/index.ts`** | HTTP：导出 **`createApp` / `gracefulShutdown`**，并启动监听。 |
| **`prisma/`** | `schema.prisma`、迁移与 seed。 |

## `import` 说明

- **`pnpm build`**：`tsc` → **`tsc-alias`** → **`scripts/fix-barrel-imports.mjs`**（**`dist`**：`models`、`services`、`routes`、`views`、`controllers`、`lib`、`worker` 等 **barrel**，以及 **`models/auth`、`controllers/task`、`lib/common` 等子路径** → **`…/index.js`**；其余无扩展名相对路径 → **`.js`**）。**新增**模块子目录名时，需同步脚本内 **`nestedIndexLeaves`**。
- **跨目录**优先 **`@/controllers/<模块>`**、**`@/models/<模块>`**、**`@/services/<模块>`** 等；**不写** **`/index`**。未拆分时也可继续 **`import { … } from "@/lib"`**（走根聚合）。
- **`@/worker/jobs`**：worker 侧任务入口聚合。
- 其它约定同根目录 **`project-conventions.mdc`**（路径别名、防循环等）。

## 新增功能时的习惯

- 新能力：在对应 **`controllers/<模块>/`**、**`models/<模块>/`**、**`services/<模块>/`**、**`views/<模块>/`**（按需）各增/改 **`index.ts`**；**`routes/<模块>/`** 挂 Router；在 **`src/index.ts`** 的 **`createApp`** 中 **`app.use(...)`** 挂载（与现有前缀一致）；敏感操作配 **`requireUser`** / **`requireRunner`**。
- Worker 任务逻辑放在 **`src/worker/jobs/`**，由 **`src/worker/index.ts`** 订阅队列并调用。
