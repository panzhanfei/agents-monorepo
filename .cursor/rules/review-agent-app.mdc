---
description: apps/review-agent — 审核 Agent（eslint/tsc/脚本 blocking + 读取 aiRules 的语义评审）
globs: apps/review-agent/**/*.ts
alwaysApply: false
---

# apps/review-agent

## 职责

- **第一段（确定性）**：聚合调用仓库根脚本 — `pnpm run lint`、`pnpm run check-types`、自定义脚本等；失败即 blocking。
- **第二段（模型）**：将 **`agents.config.yaml`** 中当前 **profile** 的 `aiRulesGlob`、`customerRulesDir` 与 **`.cursor/rules`** 等合并后，与 **diff / 上下文** 一并送入 Review 模型；输出 **`blocking`** vs **`warnings`**。
- **客户可改规则**：客户可在 **`REVIEW_CUSTOM_RULES_DIR` / `review.profiles.*.customerRulesDir`** 内增删改文件；建议 `REVIEW_RULES_RELOAD_EACH_RUN=true`，或改后由 orchestrator 触发一次「规则热加载」。 blocking 命令也可按 profile 调整（比单条 `REVIEW_BLOCKING_COMMAND` 更细时以 YAML 为准，env 可作全局覆盖）。
- **不**取代 CI：本地/ Runner 仍应以同一命令为准，避免 Agent 与流水线漂移。

## 配置约定

- **静态口令门禁**：修改审核 profile、`customerRulesDir`、blocking 列表等敏感变更，须在 orchestrator **确认消息包含配置的口令**后再落盘或触发新一轮评审。
- **规则路径**：以根配置 **activeProfile** 为准；客户专用目录与默认 glob **合并**，勿在应用内再硬编码第二套来源。
- **输出**：评审结果应可被 orchestrator 解析（JSON 或固定 Markdown 小节），便于 PR 评论与飞书摘要。

## 演进时的目录习惯

| 目录 | 职责 |
|------|------|
| **`src/config`** | 读取 `agents.config.yaml` 与环境变量（校验用 Zod） |
| **`src/gates`** | shell/turbo 门禁封装（超时、日志聚合） |
| **`src/review`** | LLM 评审 prompt 组装与解析 |
| **`src/routes`** | 可选 HTTP（`/health`、触发评审的 webhook） |

## 依赖

- **`@agents/pipeline-core`**：门禁步骤枚举与统一报告类型（演进时补齐）。
