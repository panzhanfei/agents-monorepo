---
description: 运行时 Skill（阶段载荷、implementationRole、stackProfile、opsMode）在仓库内的分层落点，与 docs/ARCHITECTURE §15 一致；非 Cursor 编辑器 SKILL.md
alwaysApply: true
---

# 运行时 Skill 分层（本仓编码约定）

本文 **「Skill」** 指：**脱离编辑器、由编排调度时的阶段能力与任务上下文**（见 **`docs/ARCHITECTURE.md` §15**），例如 `implementationRole`、`stackProfile`、`opsMode`、`targetStackTargets`。**不是** Cursor 里的 `SKILL.md` 技能文件。

编写或扩展这类能力时，按下列 **分层** 落代码，避免在多个 `apps/*` 各复制一套枚举或魔法字符串。

## 1. 契约层 — `packages/pipeline-core`

- **放什么**：跨 Orchestrator 与所有 Agent 共享的 **类型与常量**——如 `ImplementationRole`、`StackProfile`、`OpsMode` 的 **联合类型 / 枚举**、任务/子请求 DTO 上与 Skill 相关的字段、与 `PipelineStepKind` / `ITaskRecord` 扩展字段的 **单一事实来源**。
- **不放什么**：飞书文案、HTTP 路由、进程内提示词全文、shell 命令拼接。
- **原则**：新增 `stackProfile` 或扩展 Skill 字段时，**优先在此定义类型并导出**；各 app 只 `import type` 或引用同一常量，禁止散落裸字符串。

## 2. 编排层 — `apps/orchestrator`

- **放什么**：飞书/消息 **意图 → 内部 `action`** 的映射；对任务载荷做 **校验（如 Zod）与默认值**；在调用下游 Agent 时 **注入/透传** Skill 上下文；并发门禁、状态机允许的步骤。
- **不放什么**：具体 lint 规则、单测跑法、发布脚本实现、客户仓内的框架细节。
- **原则**：orchestrator 只保证 **边界合法 + 传递一致**（同一实现链上 coding/review/test 的 `implementationRole` + `stackProfile` 对齐见架构文档）。

## 3. 执行层 — 各 `apps/*-agent`

- **放什么**：本 Agent **如何消费** Skill 上下文（例如选择评审提示模板、限定工作目录、解释报告维度）；与 **`TARGET_WORKSPACE_PATH`** 下客户项目交互的实现。
- **不放什么**：重新定义与 pipeline-core 重复的枚举；跨 Agent 的步骤类型命名（应回归 core）。

## 4. 文档层 — `docs/ARCHITECTURE.md` §15

- **放什么**：对外 **v1 约定**（阶段与载荷表、`stackProfile` 枚举表、`opsMode` 含义）。
- **原则**：扩展枚举或改变语义时 **同步更新 §15**，或留下显式 TODO 并注明/version，避免「文档一套、代码一套」。

## 5. 与飞书侧 `action` 的区别

- 用户可见的 **指令词**（如「编码」「帮助」）映射到稳定 **`action`** / `PipelineStepKind`，见 **`docs/FEISHU_COMMANDS.md`** 与 **`docs/ARCHITECTURE.md` §8**。
- **Skill 载荷** 是 **任务内部上下文**，不必与飞书关键词一一对应；新增栈画像不需要新增用户口令，除非你方产品明确要求。

## 6. 自检（改 Skill 相关代码时）

- [ ] 新枚举或字段是否在 **`pipeline-core`** 有定义并导出？
- [ ] **`orchestrator`** 是否在出站调用前完成校验？
- [ ] 受影响的 **Agent** 是否只 **消费** 契约而非复制字面量？
- [ ] **`docs/ARCHITECTURE.md` §15** 是否已更新？

更细的包与 apps 边界见 **`pipeline-packages.mdc`**、**`orchestrator-app.mdc`** 及各 **`*-agent-app.mdc`**。
