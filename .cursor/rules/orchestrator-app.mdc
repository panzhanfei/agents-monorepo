---
description: apps/orchestrator 分层与目录职责（config、routes、clients、workers 占位）
globs: apps/orchestrator/**/*.ts
alwaysApply: false
---

# apps/orchestrator 结构与分层

编排服务承担：**飞书 / Webhook / 人工触发** → **调用 `requirements-agent` / `coding-agent` / `review-agent` / `test-agent` / `ops-agent`（HTTP 或队列）** → **群内回报**。**HTTP 实现默认使用 Node.js + Express**（见 `docs/ARCHITECTURE.md` §13）。骨架阶段仅为占位，演进时保持下列边界。

**新客户 / 无 Git**：客户可在飞书发送**本机项目路径**，任务级绑定 `TARGET_WORKSPACE_PATH`；随后在飞书发送**远端仓库 URL** 后再执行 `git init` / `remote add` / `push`（凭证走本机 SSH/token，勿在飞书明文）。开关见 `.env.example` 与 `agents.config.yaml` 的 `feishu.*`。

**发包与更改配置的验证码**：当 `security.verificationCode.enabled` 时，`requiredForActions` 所列动作须在飞书消息正文中包含与 **`security.verificationCode.staticCode`**（或优先 **`VERIFY_CODE`**）相同的静态口令；**未命中不得**调用 ops-agent 发布或改写审核/部署相关配置。

## import 路径

若目录已有 **`index.ts` barrel**，业务代码从**目录别名或 barrel**引入，避免深层相对路径泄漏实现文件（后续接入 `@/*` 或 `src/index` 时再统一）。

## 请求链路（演进目标）

1. **`src/routes`**：挂载路径、选用中间件、绑定 handler；不写领域编排细节。
2. **`src/handlers` 或 `src/controllers`**：解析请求、校验入参、调用 `services`。
3. **`src/services`**：用例编排；依赖 `clients` 与 `config`。
4. **`packages/pipeline-core`**：步骤枚举、类型与可在 CI/Worker 复用的纯函数。

长耗时 Job（全量测试、发布）不要阻塞 HTTP：后续用队列或子进程 + 状态表，`src/workers` 消费。

## 目录职责（占位）

| 目录 | 职责 |
|------|------|
| **`src/config`** | 环境变量校验与聚合导出（推荐 Zod）；不写业务分支 |
| **`src/clients`** | 外部系统客户端（Git、飞书、对象存储）初始化 |
| **`src/middlewares`** | Express 中间件（安全头、`json` 解析限制、按需限流） |
| **`src/routes`** | Express 路由表（`Router` 按域拆分） |
| **`src/services`** | 编排逻辑 |
| **`src/workers`** | 异步任务与消费者 |

## 边界

- **`src/utils`**：无状态工具；不放长生命周期连接。
- **密钥**：只在 `config` 层读取 env，敏感不落日志。

新增对外引用时同步更新对应 barrel（与根目录「聚合导出」规则一致）。
