---
description: 密码、密钥、令牌等敏感配置须放在 .env，禁止写入源码或提交仓库
alwaysApply: true
---

# 环境与密钥（`.env`）

## 必须放进环境变量文件的内容

以下**不得**硬编码在业务代码、配置文件（除示例模板外）或文档中：

- 密码、数据库连接串、私钥、证书内容
- API Key、Access Token、Refresh Token、Webhook Secret、签名密钥
- **运维静态验证码**（`VERIFY_CODE` / `security.verificationCode.staticCode`）、SSH 私钥
- 第三方服务密钥（云厂商、支付、邮件、OSS 等）
- 其他**仅生产/预发应知**或**按环境变化**的敏感配置

## 做法

- 将实际值写在 **`.env` / `.env.local`** 等（以应用约定为准），通过 `process.env` 或统一配置加载读取。
- **不要**提交含真实密钥的文件；仅 **`\.env.example`** 作为占位说明。
- 新增变量时在 **`.env.example`** 增加同名占位与说明。
- **不要**在 PR、issue、日志、注释里粘贴真实密钥。

## 反例与正例

```typescript
// ❌ 禁止：密钥出现在源码中
const apiKey = 'sk-live-xxxx';

// ✅ 正确：从环境读取，缺省时在启动阶段校验并给出明确错误
const apiKey = process.env.MY_SERVICE_API_KEY;
```

若能力必须「默认开发用假值」，使用**明显占位**且仅限本地/测试，并仍通过 `process.env` 注入。
