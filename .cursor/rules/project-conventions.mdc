---
description: TS 风格、interface.ts 与 I 前缀类型命名、聚合导出、packages、新增能力前先查复用（对齐 sentinel-monorepo 约定）
alwaysApply: true
---

# 项目约定（agents-monorepo）

## TypeScript 与函数风格

- 默认使用**箭头函数**编写模块级函数；仅当框架必须用 `function` 时再使用。
- **导出 API**、**对外 props**、**hook 返回值**与边界载荷写**显式类型**；避免 `any`，必要时 `unknown` 再收窄。
- 局部可从字面量明显推断时可依赖推断；**边界处**仍写清类型。

## 不在自定义函数内再声明函数

- 业务代码中不要在函数内部再声明长的嵌套子函数（不利于测试与复用）。
- **例外**：框架回调、`map`/`then` 一行级短回调；体积大则外提成模块级具名函数或工厂文件。

## 类型拆分与 `interface.ts`

- 声明累积较多行时抽到同 feature 的 **`interface.ts`**。
- **命名**：对外接口与对象类型 **`I` + PascalCase**（如 `IOrchestratorContext`）。
- **导出**：`export interface I...` / `export type`，业务文件 `import type`。

## 新增能力：先查复用再实现

- **编排与流水线**：跨通道（飞书 / Git / CI）的共享逻辑放在 **`packages/pipeline-core`** 或后续新增 **`packages/*`**，不要在多个 `apps/*` 复制。
- **HTTP 编排入口**：`apps/orchestrator` 负责飞书/Webhook、任务状态与调用各 **Agent**（`requirements-agent`、`coding-agent`、`review-agent`、`test-agent`、`ops-agent`）；**不要**把编码/审核/测试执行/发布细节堆在 orchestrator，应分别落在对应 `apps/*` 或 `packages/*`。
- **运行时 Skill 分层**（`implementationRole` / `stackProfile` / `opsMode` 等）：契约在 **`packages/pipeline-core`**，编排校验与透传在 **`apps/orchestrator`**，消费逻辑在各 **`apps/*-agent`**；约定见 **`runtime-skills-layering.mdc`** 与 **`docs/ARCHITECTURE.md` §15**。

## Monorepo：共享能力放在 `packages/`

- 多应用或多 Job 共用的类型、客户端封装、门禁适配器放在 **`packages/<包名>/`**。
- 应用通过 workspace 依赖引用。

## 聚合导出（`index.ts` / barrel）

- 新增对外 API 时在对应层级维护 **`index.ts`** 聚合导出。
- **测试文件**不要进业务 barrel。
- **`packages/*`**：遵循各包 `package.json` `exports`。

## 改完代码后必须自测（再收口给用户）

- **本仓内**：对改动的包至少跑 **`pnpm --filter <包名> test`** 与包内 **`tsc --noEmit`**（或 `turbo run check-types`，若你为某包配置了该脚本）；动到 **`pnpm dev` / 端口 / Turbo 与多 app 启动** 时，再跑一轮 **`pnpm exec turbo run dev`**（短时、无 ngrok）做冒烟，确认无 **`EADDRINUSE`**、关键 **`/health`** 可访问；**未跑通不得**对用户说「已好 / 没问题」。
- **飞书 Webhook / 编排飞书链路**：收口前跑一次 **`pnpm verify:feishu`**；若改需求分析下游可 **`SMOKE_REQUIREMENTS_FULL=1 ... pnpm verify:feishu`**。
- **全项目流转日志（第二终端）**：开发环境各服务默认把 JSON 日志写入仓库 **`logs/<service>.log`**（可通过 **`AGENTS_LOG_DIR`** 改路，**`AGENTS_LOG_DISABLE=1`** 关闭）。另开终端运行 **`pnpm logs`** 即 **`tail -f` 聚合**这些文件；**未跑 dev 时** `pnpm logs` 会等待进程创建日志。
- **环境所限**（如无终端、沙箱不能起服务）时：**明说**未跑哪一步、用户本地需补跑什么命令。

各应用细则见：`orchestrator-app.mdc`、`requirements-agent-app.mdc`、`coding-agent-app.mdc`、`review-agent-app.mdc`、`test-agent-app.mdc`、`ops-agent-app.mdc`、`pipeline-packages.mdc`、`runtime-skills-layering.mdc`。
