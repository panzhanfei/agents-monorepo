---
description: TS 风格、interface.ts 与 I 前缀类型命名、聚合导出、packages、新增业务前先查复用
alwaysApply: true
---

# 项目约定

## TypeScript 与函数风格

- 默认使用**箭头函数**编写组件、hooks、工具函数与模块级函数；仅当框架或场景必须用 `function` 声明时（例如部分生成器、依赖提升等）再使用 `function`。
- 对**导出 API**、**对外 props**、**hook 返回值**，以及含义不直观的参数/返回值，使用**显式类型**。避免 `any`；需要时用 `unknown` 再收窄。
- 局部若类型可从字面量或泛型明显推断（如 `const n = 1`），可依赖推断；但在**边界处**（导出、props、RPC 载荷等）仍应写清类型。

```typescript
// 推荐
export const buildUser = (id: string): User | null => { ... };

// 新代码中尽量避免
export function buildUser(id: string) { ... }
```

## 不在自定义函数内再声明函数

- 业务代码、路由处理函数、服务方法等**自写逻辑**中：不要在函数内部再 `function foo() {}` 或 `const foo = () => {}` 声明**嵌套子函数**（易拉长作用域、不利单测与复用）。
- **例外**（可保留内层函数或回调）：
  - **框架/库约定**的 API：如 React 的 `useState`/`useEffect`、各 UI 库传入的 `render`/`children` 工厂等。
  - **原生高阶**用法：`array.map((x) => ...)`、`.then(...)` 等**无法外提**的一行级回调，宜保持极短；若体量大则**外提成模块级**具名函数或**工厂**（如 `createXxxPublish(args)` 放在同 feature 的独立文件）。
- 需要「请求级闭包」时，优先**模块内工厂**（如 `createChatSsePublish`）在**子文件**返回具名能力，**不在** 200 行的 handler 里长嵌套。

## 类型拆分与 `interface.ts`

- 当**组件 props**、**hook 入参/返回值**、**方法参数与返回**、**视图/表格行数据**等 TypeScript 声明**累计较多行**（大致 ≥ ~30 行或单文件可读性明显下降）时：将具名类型抽到**同 feature 目录**下的 **`interface.ts`**（与仓库中 `components/.../interface.ts` 模式一致），主文件只保留实现与必要的窄类型。
- **文件名**：优先使用 **`interface.ts`**；同一目录若需区分「仅类型」与「运行时常量」，可保留 `constants.ts` 等，但**不要**把大段接口堆回业务文件。
- **命名**：对外使用的**接口与具名对象类型**统一 **`I` + PascalCase**，名称与对应**组件 / hook / 方法 / 领域数据**的英文含义一致（取整体概念的首段 PascalCase，而非单词首字母缩写）。
  - 组件 `AgentTerminal` 的 props → `IAgentTerminalProps`
  - 列表行「审计足迹」→ `IFootprintRow` 或 `IAuditFootprintRow`
  - 函数 `mergeChatHistory` 的入参/出参 → `IMergeChatHistoryParams`、`IMergeChatHistoryResult`
  - API / BFF 响应体 → `IChatSessionResponse` 等
- **导出**：在 `interface.ts` 内 `export interface I...` 或 `export type I...`（对象形状、联合等具名类型）；业务文件 `import type { ... } from "./interface"`。
- **渐进迁移**：历史文件若无 `I` 前缀，**新改动的导出类型**优先按本规则命名；不必为纯重构一次性改全仓库。

## 新增业务：先查复用再实现

- **API 与 DTO**：**合并、清洗、多数据源拼装** 优先在 **`apps/api`** 的**用例级路由/模块**（或未来的 `services/`）中完成；**`apps/web`** 经 **`src/api/`** 调用并做**轻量映射**即可。
- **每次新增业务**（新页面、新接口、新 hook、新组件）在落代码前：在 monorepo 内**检索是否已有**同类能力——**`apps/web`** 的 `pages` / `hooks` / `components` / `api`，**`apps/api`** 的 `routes` / `lib` / `jobs`，以及 **`packages/*`**（若已建：`ui`、`auth`、`database` 等）。
- 若存在**语义接近、边界清晰、可参数化**的实现：优先**复用**；若两处已出现重复或第三处将复制：评估**抽离**——组件/hook 上浮到 **`packages/ui`** 或当前应用内更高层 `components` / `hooks`；纯函数进 `utils` 或 **`packages`** 下合适包。
- 避免在多个 `apps/*` 各写一套「只差配置」的拷贝；跨应用共性必须符合上文 **`packages/`** 约定。

## Monorepo：共享服务放在 `packages/`

- 具有**共享服务能力**的模块（数据访问、外部 API、鉴权辅助、缓存、「数据库」层、跨应用业务逻辑等）应放在 **`packages/<包名>/`**，不要在多个 `apps/*` 中重复实现。
- **前后端共用的 JSON / API 契约类型**（领域 DTO、列表项、`…Response` / `…Body` 等仅类型声明）放在 **`packages/shared-types`**（包名 **`@agents/shared-types`**）；**`apps/web`**、**`apps/api`** 通过 **`workspace:*`** 依赖引用，**禁止**在两处各维护一套同名形状。
- 应用通过 **workspace 依赖**引用这些包；应用代码侧重 UI、路由与组装。
- 新增共享服务包时：遵循本仓库已有包的目录与构建方式（构建工具、`package.json` 的 `exports`、tsconfig 引用等）。

## 聚合导出（`index.ts` / barrel）

本约定适用于 **`apps/*`、`packages/*`** 等 TypeScript 源码树：**跨目录引用优先走目录级 barrel**，便于统一导入路径与评审「模块对外边界」。

### 必须维护 `index.ts` 的典型目录

- **`apps/api/src`**：`config/`、`lib/`、`middleware/`、`routes/`、`queue/`、`jobs/`（以及后续新增的同级「多文件领域目录」）。
- **`apps/web/src`**：`api/`、`auth/`、`pages/`、`utils/`、`routes/`（以及后续 `components/`、`hooks/` 等多文件目录）。

### 导入约定

- **路径别名 `@/`**：**`apps/web`**、**`apps/api`** 的源码树根 **`src/`** 映射为 **`@/`**（例如 **`import { x } from "@/lib"`**）；**跨目录**引用 barrel 时优先 **`@/lib`**、**`@/routes`** 等，**不写** **`/index`**。**`apps/web`**：`vite.config.ts` **`resolve.alias`** 与 **`tsconfig.app.json`** **`paths`** 对齐。**`apps/api`**：**`tsconfig.json`** **`paths`**；**`pnpm build`**：`tsc` → **`tsc-alias`**（把 **`@/`** 改写为 **`dist/`** 内相对路径）→ **`fix-barrel-imports`**（将 **`config` / `lib` / `middleware` / `routes` / `queue` / `jobs`** 等 barrel 裸路径补上 **`/index.js`**，供 **`node`** 加载）；**新增**同级 barrel 根目录名时需同步 **`scripts/fix-barrel-imports.mjs`** 内集合。**同一目录内**：**`apps/web`** 引用兄弟 **`.ts` / `.tsx`** 模块宜 **`./foo`**（**不写** **`.js`**）；**`apps/api`** 仍 **`./foo.js`**，与 **`tsc` 产出**一致。**不经别名**绕 barrel。Monorepo 包仍走 **`package.json` `exports`**。
- **目录内部**：实现文件之间仍优先 **同级相对路径**（**`apps/web`**：`./client`、`./interface`；**`apps/api`**：`./client.js`、`./interface.js`），**不要**在同目录实现里为引用兄弟文件而去绕一圈自己的 `index.ts`，避免 **barrel 自引用**。
- **对外边界**：新增可被其它目录使用的符号时，在 **`index.ts`** 里 **`export { … } from './foo'`**（**`apps/web`**）或 **`from './foo.js'`**（**`apps/api`**）按需聚合；**仅目录内部使用**的实现细节可不写入 barrel。
- **不要进 barrel**：仅供测试的 helper / fixture / mock、`.test.` / `.spec.` / `__tests__/` 下模块——**不要**写入业务用 **`index.ts`**。

### 防循环依赖（硬规则）

若从对方的 **`index.ts`** 导入会导致 **运行时循环**，允许（且应当）改为从对方的 **叶子模块**做 **仅类型**引用——典型为 **`interface.ts`**（**`apps/web`** 可用 **`import type { … } from "@/auth/interface"`**）；**禁止**为此放宽为有副作用的环。

### 其它

- **配置**：在 **`config/index.ts`**（或包约定入口）汇总再导出（实现仍可落在 `env.ts` 等文件中）。
- **路由（后端）**：新路由文件写在 **`routes/`**，并在 **`src/server.ts`** 的 `createApp` 中挂载；**路由聚合导出**放在 **`routes/index.ts`**。
- **`packages/*`**：遵循 **`package.json` `exports`** 与 **`src/index.ts`**（若存在），原则与本条一致。
- **文件名并列**：若「**单文件**」与「**同名目录**」并列（如 `proxy.ts` 与 `proxy/`），解析器可能优先解析文件；按需使用子路径或明确 `./proxy/index.js`，避免误指向。

## `apps/*` 源码语言

- **`apps/web`**、**`apps/api`** 业务与源码目录下的实现文件**一律为 `.ts` / `.tsx`**（配置工具链文件如 `eslint.config.ts`、`vite.config.ts` 等同理）；**不要**新增 `.js` / `.jsx` 业务源码。

**`apps/web`** 以 `.cursor/rules/web-react.mdc` 为准；**`apps/api`** 以 `.cursor/rules/api-node.mdc` 为准。
