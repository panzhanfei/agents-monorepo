---
description: apps/test-agent — 全量测试执行、日志与覆盖率聚合、测试报告摘要（对接飞书由 orchestrator 转发）
globs: apps/test-agent/**/*.ts
alwaysApply: false
---

# apps/test-agent

## 职责

- 读取 **`agents.config.yaml`** 中的 **`pipeline.fullTestCommand`**（及后续扩展字段），在受控环境中执行 **全量** 单元测试、e2e、覆盖率等。
- 将 stdout/stderr、退出码与产物路径规整为 **`ITestReport` 类结构化结果**（演进时在 `pipeline-core` 定义），供 orchestrator **发到飞书群**。
- **不**自行决定「合并代码」：是否进入测试阶段由 orchestrator / 状态机约束；测试失败则上报并阻断后续 **ops-agent**。

## 边界

- 与 **review-agent** 区分：review 侧重 **变更门禁 + 语义规则**；test-agent 侧重 **可重复的整仓测试执行**。
- 长耗时任务使用子进程 + 超时；避免在 HTTP handler 内同步阻塞过久（可异步 job + 轮询）。

## 依赖

- **`@agents/pipeline-core`**：`qa_full_suite` 等步骤枚举与报告类型。
