---
description: packages/pipeline-core 等共享包的导出、版本与无副作用纯函数偏好
globs: packages/**/*.ts
alwaysApply: false
---

# packages/* 约定

## 职责

- **`@agents/pipeline-core`**：流水线步骤类型、**`ITaskStore` 任务持久化契约**、门禁与报告结构的共享内核；避免依赖 express 本体（HTTP 错误类型在 `@agents/http-errors`）。
- **`@agents/logger`**：结构化 JSON 日志（`service`、`taskId` 等 meta）与 **monorepo 根 `.env` 加载**（`loadMonorepoEnvFromEntry`）；**避免在 import 时读 env**（除 `loadEnv` 显式调用外无环境副作用）。
- **`@agents/http-errors`**：`AppError`、Express **统一错误处理**、**`helmet`** 安全头、404；依赖 `@types/express`，与 **Express 4** 搭配使用。
- 后续新增包（如 `feishu-client`、`git-adapter`）应保持 **单侧边界**：向外暴露稳定接口，向内封装第三方 SDK。

## 模块形状

- **`package.json` `exports`**：对外入口收敛到 `src/index.ts`（构建后为 `dist/index.js`）。
- **副作用**：库代码避免在 import 时启动网络或读 env；初始化由 orchestrator 显式调用 factory。
- **测试**：每个包自带 `vitest`；门禁脚本优先调用 workspace 脚本而非猜测路径。

## 与 apps 的边界

- apps 可做 IO 与进程；packages 偏向**可测试、可重用**的逻辑。
- 禁止 packages 反向依赖 `apps/*`。
