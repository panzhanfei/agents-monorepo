# 后端 API 根地址（浏览器访问）。参见 apps/web/src/api/client.ts 解析顺序。
#
# 可选：填写后优先使用（线上 / 预发）。本地开发若不连线上，请留空或删除该行。
# VITE_API_BASE_ONLINE=https://api.example.com
#
# 未配置线上地址时使用（通常为本地 API）。
# VITE_API_BASE=http://127.0.0.1:3000
#
# 可选：覆盖本机 Agents 入口根地址（须与浏览器能访问的地址一致，默认 http://127.0.0.1:3998）。
# VITE_RUNNER_BASE=http://127.0.0.1:3998
